# Error-count gate reads the docstring by regex; a miss raises AttributeError, not an assertion
kind: todo
created: 2026-09-15T07:50Z

- 2026-09-15T07:51Z Panel finding TST-F5, deferred. tests/test_errors.py reads 'Codes declared here: N.' out of the module docstring with a regex and calls .group(1) on the result; if the sentence is reworded the match is None and the failure is an AttributeError rather than a readable assertion. The gate itself works -- it caught the count sitting at 23 when there were 25, on its first run. Fix is a named constant in errors.py that the docstring and the test both cite, so there is nothing to parse.
